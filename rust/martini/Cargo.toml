[package]
name = "martini"
version = "0.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "RTIN mesh generation for terrain data based on the Martini algorithm"
readme = "README.md"
keywords = ["terrain", "mesh", "rtin", "martini", "heightmap"]
categories = ["algorithms", "graphics", "science::geo"]

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]

[dev-dependencies]
approx = "0.5"
image = "0.25"
